include ../make.common

HEADERS=child_reader.h file_manip.h string_manip.h filename_match.h

SOURCES=child_reader.cpp file_manip.cpp string_manip.cpp filename_match.cpp

OBJECTS=child_reader.o file_manip.o string_manip.o filename_match.o

TARGET=libutil++.a

all: $(TARGET)
install:
uninstall:
clean:
	rm -rf .deps $(OBJECTS) $(TARGET) 

$(TARGET): $(OBJECTS)
	$(AR) cr $@ $^

include ../make.deps
