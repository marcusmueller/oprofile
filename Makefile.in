# $Id: Makefile.in,v 1.22 2001/08/16 01:57:58 movement Exp $

BKCFLAGS=@BKCFLAGS@ @MODVERSIONS@
MODINSTALLDIR=@MODINSTALLDIR@/oprofile
MKDIR_P=mkdir -p
CC=@CC@
LD=@LD@

.PHONY: all install clean distclean tags

all: oprofile.o
	$(MAKE) -C dae
	$(MAKE) -C pp
	$(MAKE) -C gui

maintainerclean: distclean
	rm -f configure

distclean: clean
	rm -f Makefile dae/Makefile pp/Makefile tags

# broken 
dist:
	mkdir tmpdist ; \
	cd tmpdist ; \
	cvs checkout oprofile ; \
	cd oprofile ; \
	(find -type d -name CVS | xargs rm -rf) ; \
	autoconf ; \
	./configure ; \
	cp doc/Makefile doc/Makefile.tmp ; \
	make distclean ; \
	cd doc && make && rm -f Makefile && cd .. ; \
	cd .. ; \
	mv oprofile oprofile-@VERSION@ ; \
	tar cvfz ../oprofile-@VERSION@.tar.gz oprofile-@VERSION@ ; \
	rm -rf oprofile-@VERSION@ ; \
	cd ..  ; \
	rm -rf tmpdist \

tags:
	ctags -R

install: oprofile.o
	-$(MKDIR_P) $(MODINSTALLDIR)
	cp oprofile.o $(MODINSTALLDIR)
	depmod -a
	$(MAKE) -C dae install
	$(MAKE) -C pp install
	$(MAKE) -C doc install 
	$(MAKE) -C gui install

clean:
	rm -f *.o config.status config.log config.cache
	$(MAKE) -C dae clean
	$(MAKE) -C pp clean
	$(MAKE) -C doc clean
	$(MAKE) -C gui clean

# FIXME: move NMI code into separate to allow -march=ppro for that
BKCFLAGS += -pipe -D__KERNEL__ -DMODULE -Wall -Wstrict-prototypes -Wunused -O2 -fomit-frame-pointer -fno-strict-aliasing
KCFLAGS := $(BKCFLAGS) -march=i686
ASMFLAGS := -D__ASSEMBLY__ -DMODULE -D__KERNEL__ -traditional

oprofile.o: op_init.o oprofile_c.o oprofile_nmi.o op_syscalls.o op_events.o
	ld -r -o $@ op_init.o oprofile_c.o oprofile_nmi.o op_syscalls.o op_events.o

oprofile.h: op_user.h version.h

op_init.o: op_init.c oprofile.h
	$(CC) $(BKCFLAGS) -c -o $@ $<

oprofile_c.o: oprofile.c oprofile.h
	$(CC) $(KCFLAGS) -c -o $@ $<

op_syscalls.o: op_syscalls.c oprofile.h
	$(CC) $(KCFLAGS) -c -o $@ $<

oprofile.s: oprofile.c oprofile.h
	$(CC) $(KCFLAGS) -S $<

op_events.o: op_events.c
	$(CC) $(KCFLAGS) -c -o $@ $<

oprofile_nmi.o: oprofile_nmi.S
	$(CC) $(ASMFLAGS) -c -o $@ $<
