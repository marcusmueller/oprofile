include ../make.common

HEADERS=popt_options.h

SOURCES=popt_options.cpp

OBJECTS=popt_options.o

TARGET=libopt++.a

all: $(TARGET)
install:
uninstall:
clean:
	rm -rf .deps $(OBJECTS) $(TARGET) 

$(TARGET): $(OBJECTS)
	$(AR) cr $@ $^

include ../make.deps
