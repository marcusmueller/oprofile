include ../make.common

HEADERS=op_print_event.h op_mangling.h
 
SOURCES=op_print_event.cpp op_mangling.cpp

OBJECTS=op_print_event.o op_mangling.o

TARGET=libop++.a

all: $(TARGET)
install:
uninstall:
clean:
	rm -rf .deps $(OBJECTS) $(TARGET) 

$(TARGET): $(OBJECTS)
	$(AR) cr $@ $^
 
include ../make.deps
